# Demand Service

Service that exposes demand estimates and endpoints used by the Decision-Orchestration Agent.

Key endpoints:
- GET /demand
- GET /demand/{id}
- POST /demand/{id}/recommendation

Reads/writes to `database.demand`.

Placeholder: add implementation and API docs.