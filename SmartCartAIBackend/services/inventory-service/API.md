# Inventory Service API

## GET /inventory
Returns a list of inventory items.

## GET /inventory/{id}
Returns details for a single inventory item.

## POST /inventory/{id}/flag
Updates flag status for an inventory item.

## POST /inventory/{id}/recommendation
Accepts a recommendation payload from the decision agent.

(These are placeholders; add request/response schemas and examples.)